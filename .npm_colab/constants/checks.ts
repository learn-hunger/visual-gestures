export enum EChecks {
    ENUM_IDENTIFIER,
    TYPE_IDENTIFER,
    INTERFACE_IDENTIFIER,
    ABSTRACT_IDENTIFIER,
    CLASS_IDENTIFIER,
    NAMESPACE_IDENTIFER,

    CONST_IDENTIFIER,
}

export class ErrorExist {
    public static state = false;
}