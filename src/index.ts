import { AVgCommon } from "./app/shared/blueprints/vg-cursor";
import { INormalizedLandmark } from "./app/utilities/vg-types-handlandmarks";

export class Main extends AVgCommon {
  detect(landmark: INormalizedLandmark[][]): void {}
}
